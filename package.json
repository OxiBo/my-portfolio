{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html",
    "build": "parcel build index.html --public-url /my-portfolio/",
    "sass": "sass",
    "parcel": "parcel build index.html",
    "deploy": "gh-pages -d dist"
  },
  "author": "OxiBo",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "parcel-bundler": "^1.12.4",
    "sass": "^1.27.0"
  }
}
